╔══════════════════════════════════════════════════════════════╗
║        CAMERA SERVER QUICK REFERENCE CARD                    ║
╚══════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────┐
│  🎯 DEVELOPMENT MODE (Testing with Expo Go)                  │
└──────────────────────────────────────────────────────────────┘

  Start Camera:
    cd ~/Internet-of-Tsiken-v2/"yolo object detection"
    python stream_server.py

  Start Expo:
    cd ~/Internet-of-Tsiken-v2
    npm start

  Stop Camera:
    Press Ctrl+C


┌──────────────────────────────────────────────────────────────┐
│  🚀 PRODUCTION MODE (APK Deployment)                         │
└──────────────────────────────────────────────────────────────┘

  ONE-TIME SETUP:
    cd ~/Internet-of-Tsiken-v2/"yolo object detection"
    ./install_service.sh

  BUILD APK:
    cd ~/Internet-of-Tsiken-v2
    eas build --platform android --profile preview

  INSTALL APK:
    Download → Install on phone → Open app → Camera auto-connects


┌──────────────────────────────────────────────────────────────┐
│  🔧 SERVICE MANAGEMENT                                       │
└──────────────────────────────────────────────────────────────┘

  Check Status:       sudo systemctl status yolo-camera
  View Logs:          sudo journalctl -u yolo-camera -f
  Restart Service:    sudo systemctl restart yolo-camera
  Stop Service:       sudo systemctl stop yolo-camera
  Start Service:      sudo systemctl start yolo-camera
  Disable Auto-Start: sudo systemctl disable yolo-camera
  Enable Auto-Start:  sudo systemctl enable yolo-camera
  Uninstall:          ./uninstall_service.sh


┌──────────────────────────────────────────────────────────────┐
│  🧪 TESTING COMMANDS                                         │
└──────────────────────────────────────────────────────────────┘

  Test Server Health:
    curl http://localhost:5000/status

  View Current Detections:
    curl http://localhost:5000/detections

  Test From Network:
    curl http://192.168.68.134:5000/status
    curl http://rpi5desktop.local:5000/status


┌──────────────────────────────────────────────────────────────┐
│  🌐 ACCESS URLS                                              │
└──────────────────────────────────────────────────────────────┘

  Localhost:      http://localhost:5000
  Local IP:       http://192.168.68.134:5000
  Hostname:       http://rpi5desktop.local:5000  (recommended)


┌──────────────────────────────────────────────────────────────┐
│  🐛 QUICK FIXES                                              │
└──────────────────────────────────────────────────────────────┘

  Service won't start:
    sudo journalctl -u yolo-camera -n 50
    pip install -r requirements.txt
    sudo systemctl restart yolo-camera

  Camera in use:
    pkill -f stream_server.py
    pkill -f libcamera
    sudo systemctl restart yolo-camera

  APK can't connect:
    sudo systemctl status yolo-camera
    sudo ufw allow 5000
    curl http://localhost:5000/status


┌──────────────────────────────────────────────────────────────┐
│  📋 THE PROCESS FLOW                                         │
└──────────────────────────────────────────────────────────────┘

  DEVELOPMENT:
    1. Manual start camera → 2. npm start → 3. Test → 4. Stop

  PRODUCTION:
    1. ./install_service.sh (ONE TIME)
    2. eas build
    3. Install APK
    4. DONE - camera runs forever!


┌──────────────────────────────────────────────────────────────┐
│  ⚡ KEY DIFFERENCES                                          │
└──────────────────────────────────────────────────────────────┘

  Development:
    ✗ Manual start/stop required
    ✗ Stops when terminal closes
    ✗ Doesn't survive reboot
    ✓ Good for testing changes

  Production:
    ✓ Auto-starts on boot
    ✓ Runs in background 24/7
    ✓ Survives reboots
    ✓ Auto-restarts if crashes


┌──────────────────────────────────────────────────────────────┐
│  🎬 GETTING STARTED                                          │
└──────────────────────────────────────────────────────────────┘

  Right Now (Testing):
    Just run: python stream_server.py

  Ready for APK:
    1. Run: ./install_service.sh
    2. Verify: sudo systemctl status yolo-camera
    3. Build APK: eas build --platform android
    4. Install APK on phone
    5. Forget about camera server - it just works!


╔══════════════════════════════════════════════════════════════╗
║  📖 Full Guide: See DEPLOYMENT_GUIDE.md                      ║
╚══════════════════════════════════════════════════════════════╝
